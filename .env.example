# Optional host-port overrides for machines already running other projects.
# Container-to-container ports remain unchanged.
API_GATEWAY_HOST_PORT=3100
CLIENT_PORTAL_HOST_PORT=8180
DRIVER_PORTAL_HOST_PORT=18081

# Demo JWT configuration. Use a long random secret outside classroom demos.
JWT_SECRET=swiftlogistics-secret-key-2026
JWT_EXPIRES_IN=8h
